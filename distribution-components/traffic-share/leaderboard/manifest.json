{
  "name": "traffic-share/leaderboard",
  "protoAnchor": "#ts-chart-card [data-dist-sub-pane=\"store\"] .leaderboard-table#leaderboard-table-store (fragment root = the .leaderboard-table div itself — loader stamps data-component on it, no wrapper; parent .traffic-perf-* pane chrome stays in chart-card markup, its CSS carries here)",
  "protoSource": [
    "analytics-dashboard-combined/distribution-traffic.html — store pane leaderboard div, verbatim",
    "analytics-dashboard-combined/js/distribution/distribution.js — renderLeaderboard / renderLeaderboardInto / bindStoreTabInteractions / haversineDistance + LEADERBOARD_BRAND_COLORS, verbatim; page `elements` cache shrunk to the one entry read; StoreMap calls kept verbatim behind typeof guards (live again at store-map carve); entry seam window.DistLeaderboard.build() = the leaderboard half of proto DistributionTraffic.buildStorePane()",
    "analytics-dashboard-combined/css/distribution.css — CARRIED blocks: dist:647 unified numeric badge re-scoped to .lb-rank, dist:2708 traffic-perf panel family, dist:2779 leaderboard-table + lb-* family, dist:3583/3621 responsive rows; source order preserved (.lb-rank two-block cascade)",
    "analytics-dashboard-combined/js/data/* — snapshot extended with trafficShareMetrics.storeLeaderboard + entities.stores + competitorStores (data/generate-traffic-share-data.mjs)"
  ],
  "angularTarget": "ideal-sale-circular/src/app/analytic-dashboard/components/distribution-view/traffic-share-leaderboard",
  "backing": "primeng(treetable)",
  "style": "module",
  "oracle": "store pane is lazy-built on first tab show — oracle entry clicks the By Store sub-tab on BOTH sides before diffing, crops to .traffic-perf-panel (map half renders empty both sides until the store-map carve; strip viz-inactive on the ancestor card).",
  "ux928Items": [
    "#16 By Store → Map: p-treetable conversion (native expand + sort), rename tab label, side-panel reorg — conversion follows the verbatim carve after oracle-green"
  ],
  "notes": "Rows render lazily via window.DistLeaderboard.build() from the chart-card store hook. Locked to Our Stores view ('ours'); the competitors view of renderLeaderboard is carried verbatim for the Angular twin's view input. Angular twin takes sort/view as inputs, row-select + expand as outputs (map seam).",
  "status": "converted to p-treetable (2026-07-02, decided: now not at dial) — verbatim carve was oracle 10/10 green first (git history); post-conversion baseline pixel entry EXPECTED-fail, gate = parity-oracle/functional/leaderboard-treetable.mjs 19/19 PASS (anatomy, expand/collapse, Share sort desc/asc + aria-sort, single select, sticky header, console clean) + sign-off screenshot out/leaderboard-treetable-signoff.png. DOM = real v20.4 TreeTable anatomy (TreeTableStyle.classes + TreeTableToggler template from ISC node_modules): root p-treetable p-component + hoverable/scrollable/flex-scrollable, table-container > table > thead th.p-treetable-header-cell (+ p-sortable-column), toggler button.p-treetable-toggler marginInlineStart=level*16px w/ data-p-icon chevrons, primeng/icons svgs verbatim. Density via --p-treetable-* tokens (Angular [dt] analog); NO-TOKEN rules marked in leaderboard.css. Share sort = behavior ADD (#16); rank badge + pips carried inside cells. Awaiting Adam/Max sign-off → freeze reference into baselines/"
}
